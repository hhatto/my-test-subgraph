type IncubatorAuthenticate @entity {
  id: ID!
  market: Bytes! # address
  property: Bytes! # address
  githubRepository: String
}

type IncubatorFinish @entity {
  id: ID!
  account: Bytes! # address
  githubRepository: String
  Status: Int
  Reward: BigInt
}

type IncubatorTwitter @entity {
  id: ID!
  TwitterId: String
  githubRepository: String
}
